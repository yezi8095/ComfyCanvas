# 亿幕画布（ComfyCanvas）项目迁移交接记录

> **最新交接补充（2026-08-17，优先阅读）**
>
> - 项目目前的唯一开发目录是 `D:\\Codex\\ComfyCanvas-workspace`，不要再从 C 盘旧目录或历史 `target-r*` 目录继续开发。
> - GitHub `main` 当前公开最新提交仍是 `54c598e`（Nano Banana / Midjourney）。本地还有**未提交**的最新开发改动：`src/App.tsx`、`src/app.css`、`src-tauri/src/main.rs`，以及本交接文档本身。接手后先运行 `git status -sb`，绝不能直接 reset 或覆盖这些改动。
> - 未提交改动包括：在线服务“测试连接”功能与更易读的鉴权/额度/模型错误提示；将 Google 图片生成配置校正；取消开幕动画；默认 UI 改为暖黑、米白字体、低饱和铜金按钮的电影感主题。样式工作仍在进行中，绿色旧样式可能尚有残留，应继续统一但不要改变功能流程。
> - 为腾出空间已删除 `src-tauri/target/`、`dist/` 及旧安装 EXE。这些全是可生成产物，**不影响源码开发**；首次 `npm.cmd run tauri -- dev` 或构建会因 Rust 重新编译而更慢。
> - 保留了 `release\\ffmpeg.exe`（视频导出所需，未提交 GitHub，因文件超过 100MB）。不要误删。
> - 当前不要制作安装包，产品仍在开发。优先完成主题统一、真实 API 小额测试和错误处理，再由用户决定是否发布。

> 记录日期：2026-08-14
> 当前代码版本：`54c598e`（`main`）
> GitHub：<https://github.com/yezi8095/ComfyCanvas>

## 1. 项目定位

亿幕画布是一个 Windows 桌面端的 AI 创作无限画布。用户可以在画布上放置并连接：文本、图片、视频、AI 剧本、AI 图片、AI 视频、ComfyUI 工作流和导演台素材；上游节点的文字与媒体可作为下游 AI 节点的输入或参考。

技术栈：

- 前端：React 19 + TypeScript + Vite
- 桌面端：Tauri 2 + Rust
- 目标平台：Windows，NSIS 安装包
- 代码目录：`D:\Codex\ComfyCanvas-workspace`

## 2. 当前可用的核心功能

### 无限画布与素材

- 节点可拖拽、缩放、连线，支持图片、视频、文本和生成节点。
- 连线会把上游文本、图片等内容汇总给下游 AI 节点；图片可作为图生图、视频参考或多图参考。
- 媒体库负责导入、展示和复用图片/视频/音频素材。
- 视频节点采用普通可操作的视频预览形式，避免节点本身被原生视频控件锁死拖动。

### AI 节点

1. **AI 剧本生成**
   - 输入创意、人物关系、故事梗概，输出完整影视剧本。
   - 具有题材、格式、篇幅、风格、受众、语言、集数、时长、创意强度、人物小传、分镜建议等参数。
   - 图片引用可通过 `@图片` 辅助描述；图片识别结果以纯文本方式供复制、选择与编辑。

2. **AI 图片生成**
   - 支持文生图、图生图、多图参考。
   - 常用参数：比例、分辨率、数量、视觉风格、随机种子、反向提示词、提示词强度。
   - 参考图片在节点内可以叠加，生成时也会合并画布上游图片输入。

3. **AI 视频生成**
   - 支持文生视频、图生视频、首尾帧/参考图视频等模式（是否可用会随平台与模型能力联动）。
   - 常用参数：平台、模型、模式、比例、清晰度、时长、数量、音频等。
   - 已修复视频预览无限循环/控件遮挡后导致画布不可拖动的问题。

### 工作流与 ComfyUI

- 可导入、保存、扫描并复用 ComfyUI 工作流。
- 支持导出/保存 API 工作流和主流工作流 JSON。
- 工作流库会扫描可发布参数；普通用户只看到尺寸、提示词、负面提示词等常用参数，高级节点参数留在工作流内修改。
- 本地 ComfyUI 自动尝试连接 `8188`、`8189` 端口，不依赖固定安装目录。
- 自定义本地路径可设置环境变量 `YM_COMFYUI_PATH`。

### 导演台

- 具有独立素材库、脚本/导演笔记区、预览区和时间线。
- 素材库支持大量素材的独立滚动，避免挤压剪辑/预览区域。
- 竖图预览按 9:16 的视觉尺寸呈现。
- 空时间线素材默认只能拖到 5 秒位置，避免无素材时无限拉伸。
- 已修复素材排序/标签溢出面板的问题。

## 3. 已接入的模型与平台

### 视频 API（真实协议适配）

| 平台 | 模型/用途 | 输入能力 | 配置方式 |
|---|---|---|---|
| 阿里百炼·万相 | 万相视频 | 文生视频、视控制台开通能力而定 | DashScope API Key |
| 可灵 Kling | 可灵视频 | 文生视频、单图生视频 | Access Key + Secret Key，桌面端生成短期 JWT |
| 豆包·火山方舟 | Seedance/推理接入点 | 文生视频、图生视频、多图参考 | Ark API Key + 实际开通的模型/接入点 ID |

### 图片 API（真实协议适配）

| 平台 | 可选模型 | 输入能力 | 注意事项 |
|---|---|---|---|
| OpenAI Images | `gpt-image-1`、`gpt-image-1-mini` | 文生图、单图编辑 | 使用 OpenAI 兼容图片接口 |
| Google Nano Banana | `gemini-3.1-flash-image`、`gemini-3.1-flash-lite-image`、`gemini-3-pro-image`、`gemini-2.5-flash-image` | 文生图、最多 14 张参考图 | 使用 Gemini API Key；Lite/2.5 自动锁定 1K，其余 Gemini 3 图片模型可选 1K/2K/4K |
| Midjourney | `V8.1`（界面标识） | 手动命令 | 官方没有面向普通用户开放的公共 API；程序只生成/复制安全的 `/imagine` 命令，用户在官方网页或 Discord 手动提交，再把结果导回画布 |

模型和作用应当联动：视频节点只展示视频平台/视频模型，图片节点只展示图片平台/图片模型，避免用户选到不支持当前生成模式的模型。

## 4. 积分模式：当前状态与商业化边界

画布已有“本地 ComfyUI / 自带 API Key / 亿幕云端积分”的生成来源选择、云端积分预估和按平台/模型/输入输出划分的 UI 逻辑。

**但积分目前不是可直接收款运营的完整后端。** 要让它真正有商业作用，还需要单独建设服务端：

1. 用户注册、登录、账号体系；
2. 积分余额、充值订单、消费流水和退款/任务失败返还；
3. 支付接入；
4. 服务端安全保存平台 API Key，客户端不能保存平台主密钥；
5. 任务队列、回调轮询、失败重试、成本统计；
6. 文件对象存储、清理策略、内容安全、审计日志和风控；
7. 每个模型的真实成本表与积分定价表。

推荐商业模式：三种生成来源并存。用户有本地设备时用本地 ComfyUI；已有平台账号时使用自带 Key；不愿研究接口时使用亿幕云端积分。这样不会强制收费，同时保留商业化入口。

## 5. 关键代码位置

| 位置 | 作用 |
|---|---|
| `src/App.tsx` | 主画布、节点状态、连线、生成调度、在线平台配置、ComfyUI 连接与 API 调用入口 |
| `src/AiGenerationNodes.tsx` | AI 剧本/图片节点编辑器、参数面板、模型选择与能力限制 |
| `src/CloudModelCatalog.ts` | 云端积分模型目录、平台/模型能力分类 |
| `src/CloudPointsCenter.tsx` | 积分中心 UI |
| `src/WorkflowLibrary.tsx` | 工作流库导入、扫描、导出和参数发布 |
| `src/ComfyWorkflowParameters.ts` | ComfyUI 工作流参数识别与注入 |
| `src/DirectorMode.tsx` | 导演台、素材/预览/时间线编辑 |
| `src/MediaLibrary.tsx` | 媒体库 |
| `src-tauri/src/main.rs` | Rust 命令：文件处理、ComfyUI、FFmpeg、OpenAI 图像、Google 图片、可灵/豆包视频等网络调用 |
| `src-tauri/tauri.conf.json` | 桌面应用名称、窗口、打包资源配置 |

## 6. 构建、运行与发布

### 开发运行

```powershell
Set-Location D:\Codex\ComfyCanvas-workspace
npm.cmd ci
npm.cmd run tauri -- dev
```

### 检查与安装包构建

```powershell
npm.cmd run build
cargo check --manifest-path src-tauri\Cargo.toml
cargo test --manifest-path src-tauri\Cargo.toml
npm.cmd run tauri -- build
```

最近一次安装包路径：

`D:\Codex\ComfyCanvas-workspace\src-tauri\target\release\bundle\nsis\亿幕画布_0.1.0_x64-setup.exe`

安装包构建时会带入 `release\ffmpeg.exe`。仓库不会提交 FFmpeg、`node_modules`、`dist`、`target`、旧安装包、缓存或历史备份。

若只是运行裸 EXE，需要将 `ffmpeg.exe` 放在 EXE 旁边、加入系统 `PATH`，或设置 `YM_FFMPEG_PATH`。

## 7. Git 与当前版本

- GitHub 仓库：<https://github.com/yezi8095/ComfyCanvas>
- 分支：`main`
- 最新提交：`54c598e Add Nano Banana and Midjourney image modes`
- 上一批 API 适配提交：`f78371b Add Kling and Doubao video API adapters`
- 当前 Git 工作区在本记录生成前为干净状态。

发布新改动的基本命令：

```powershell
Set-Location D:\Codex\ComfyCanvas-workspace
git status -sb
git add <明确需要提交的文件>
git commit -m "简短说明"
git push origin main
```

## 8. 外部依赖与不应迁移的文件

迁移到另一台电脑/另一个项目时，应保留源码和锁文件；不要复制构建缓存。

需要自行准备：

- Node.js、Rust、Visual Studio C++ Windows 构建工具、Tauri 所需环境；
- FFmpeg（用于视频导出）；
- 独立安装/运行的 ComfyUI（如要使用本地生成）；
- 各平台自己的 API Key 与已开通模型权限。

不要上传或迁移为公开仓库内容：

- API Key、Secret Key、JWT、支付密钥；
- `node_modules/`、`dist/`、`src-tauri/target/`；
- 旧版 EXE、临时素材、运行日志、历史备份；
- 平台生成的用户作品，除非用户明确授权。

## 9. 已知限制与建议下一步

1. **云端积分后端未完成**：这是从“能演示”到“可商业使用”的最大缺口，应优先做服务端，而不是继续只加 UI。
2. **第三方 API 需逐个平台适配**：没有真正覆盖所有平台的通用生成接口。各平台的认证、异步任务、参考图格式、回调和返回结构都不同；可做通用“适配器层”，但每个平台仍至少需要一次协议实现和测试。
3. **真实账号验收未覆盖全部平台**：需要分别用可灵、火山、Gemini、OpenAI 的真实有效 Key 做小额度端到端测试，并记录模型 ID、区域、权限与报错信息。
4. **Midjourney 不应做非官方自动化**：保持手动命令/导入结果的方式，避免账号与合规风险。
5. **凭据保护**：当前密钥按本机应用配置保存；在正式商业版本前应改为系统凭据库或服务端托管，并完成日志脱敏审计。
6. **API 适配测试**：应为每个适配器补充请求构造、响应解析和错误提示的自动化测试；目前 Rust 测试可运行，但没有覆盖真实远端 API。
7. **安装体验**：可为 GitHub Releases 自动发布 NSIS 安装包并附带版本号、更新说明和 SHA256，方便另一台电脑直接下载。

## 10. 交给新项目/新助手的启动提示词

可将下面这段原样复制给新的 Codex/开发者：

```text
请接手 Windows 桌面应用“亿幕画布 / ComfyCanvas”。源码位于 D:\\Codex\\ComfyCanvas-workspace，GitHub 是 https://github.com/yezi8095/ComfyCanvas，当前 main 最新提交为 54c598e。技术栈是 React + TypeScript + Vite 前端，Tauri 2 + Rust 桌面端。请先阅读 项目迁移交接记录.md 和 README.md，再查看 git status，不要删除历史素材或执行 git reset --hard。

项目核心是 AI 无限画布、AI 剧本/图片/视频节点、ComfyUI 工作流库、导演台、媒体库和积分模式 UI。可灵与豆包视频、OpenAI 图片、Google Nano Banana 已做 API 适配；Midjourney 仅为安全的手动 /imagine 命令模式。云端积分尚缺正式服务端和支付/账户/队列系统，不能表述为已可商用收费。

修改后需至少执行 npm.cmd run build、cargo check --manifest-path src-tauri\\Cargo.toml 和 git diff --check；需要交付桌面版时执行 npm.cmd run tauri -- build。API Key 不得提交到 Git。
```
